

.. _sphx_glr_auto_examples_compare.py:


===========================
Comparison between subjects
===========================

All the subjects compared.



.. code-block:: python


    import os.path as op

    import mne

    evokeds = list()
    for subject_id in range(1, 20):
        subject = "sub%03d" % subject_id
        fname_in = op.join('..', '..', '..', 'MEG', subject,
                           '%s-ave.fif' % subject)
        evokeds.append(mne.read_evokeds(fname_in))


Evoked responses on EEG.



.. code-block:: python

    picks = mne.pick_types(evokeds[0][0].info, meg=False, eeg=True)
    for idx, evoked in enumerate(evokeds):
        for cond in range(3):
            comm = evoked[cond].comment
            evoked[cond].plot_joint(picks=picks, title='Subject %s %s' % (idx + 1,
                                                                          comm))


Topomaps



.. code-block:: python

    for idx, evoked in enumerate(evokeds):
        for cond in range(3):
            comm = evoked[cond].comment
            evoked[cond].plot_topomap(title='Subject %s %s' % (idx + 1, comm))

**Total running time of the script:**
(0 minutes 0.000 seconds)



.. container:: sphx-glr-download

    :download:`Download Python source code: compare.py <compare.py>`


.. container:: sphx-glr-download

    :download:`Download IPython notebook: compare.ipynb <compare.ipynb>`

.. rst-class:: sphx-glr-signature

    `Generated by Sphinx-Gallery <http://sphinx-gallery.readthedocs.io>`_
